---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_sssd/tasks/service.yml
# Description : service
# ------------------------------------------------------------------------------


- name: ctlabs_sssd.tasks.service.start
  service:
    name    : "{{ ctlabs_sssd.defaults.service.name }}"
    enabled : true
    state   : started

- name: ctlabs_sssd.tasks.service.oddjobd
  service:
    name    : "{{ item }}"
    enabled : true
    state   : started
  loop: "{{ ctlabs_sssd.defaults.services }}"
