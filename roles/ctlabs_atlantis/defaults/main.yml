---

# ------------------------------------------------------------------------------
# File        : ctlabs_atlantis/defaults/main.yml
# Description : main defaults
# ------------------------------------------------------------------------------

versions:
  atlantis: 0.32.0

ctlabs_atlantis:
  defaults:
    pkgs:
      debian:
        - name : git
        - name : terraform
      redhat:
        - name : git
        - name : terraform
      atlantis:
        version : "{{ versions.atlantis }}"
        bin     : atlantis
        url     : "https://github.com/runatlantis/atlantis/releases/download/v{{ versions.atlantis }}/atlantis_linux_amd64.zip"
        bindir  : /opt/atlantis
        tmpzip  : /tmp/atlantis.zip
    config:
      cfgdir   : /etc/atlantis
      datadir  : /data
      file     : atlantis.yml
      template : atlantis.yml.j2
    service:
      name     : atlantis.service
      template : atlantis.service.j2
      user     : atlantis
      group    : atlantis
    logrotate:
  os:
    - redhat7
    - redhat8
    - centos7
    - centos8
    - centos9
    - debian11
    - kali2024
