---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_gemini_cli/tasks/precheck.yml
# Description : prechecks
# ------------------------------------------------------------------------------

- name: ctlabs_gemini_cli.tasks.precheck
  block:
    - name: ctlabs_gemini_cli.tasks.precheck.supported_os
      assert:
        that: ctg_os is in ctlabs_gemini_cli.os
        fail_msg: "OS [{{ ctg_os }}] is not supported!"
  rescue:
    - name: ctlabs_gemini_cli.tasks.precheck.skip.unsupported
      meta: end_host
