---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_xrdp/tasks/service.yml
# Description : default variables
# ------------------------------------------------------------------------------

#- name: ctlabs_xrdp.tasks.service.create
#  template:
#    src  : "{{ ctlabs_xrdp.defaults.service.template }}"
#    dest : "{{ ctg_systemd_dir + '/' + ctlabs_xrdp.defaults.service.name }}"
#  notify: ctlabs_xrdp.handlers.systemd.reload

- name: ctlabs_xrdp.tasks.service.start
  service:
    name  : "{{ ctlabs_xrdp.defaults.service.name }}"
    state : started
