---
# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_zap/tests/test_zap.yml
# Description : test playbook for ctlabs_zap (syntax-check target)
# ------------------------------------------------------------------------------

- name: ctlabs_zap.test.run
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
    ctg_virtype: kvm
    ctg_systemd_dir: /etc/systemd/system
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"
