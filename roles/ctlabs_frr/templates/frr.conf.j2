{# ---------------------------------------------------------------  #}
{# File: ctlabs-ansible/roles/ctlabs_frr/templates/frr.j2           #}
{# --------------------------------------------------------------- -#}

frr version 10.0
frr defaults {{ ctlabs_frr_daemons_profile }}
hostname {{ ansible_nodename }}
log syslog informational
no ipv6 forwarding

{# BGP -#}
{% if ctg_facts.ctlabs_frr.router.bgp is defined %}
router bgp {{ ctg_facts.ctlabs_frr.router.bgp.local_asn }}
  bgp router-id {{ ctg_facts.ctlabs_frr.router.bgp.local_ip }}
  neighbor {{ ctg_facts.ctlabs_frr.router.bgp.remote_ip }} remote-as {{ ctg_facts.ctlabs_frr.router.bgp.remote_asn }}
{%   if ctg_facts.ctlabs_frr.router.bgp.nets %}
  address-family ipv4 unicast
{%     for net in ctg_facts.ctlabs_frr.router.bgp.nets %}
  network {{ net }}
{%     endfor %}
{%   endif %}
 exit-address-family
{% endif %}

{# OSPF -#}
{% if ctg_facts.ctlabs_frr.router.ospf is defined %}
router ospf
{%   for net in ctg_facts.ctlabs_frr.router.ospf.nets %}
  network {{ net }} area 0
{%   endfor %}
{% endif %}
exit
