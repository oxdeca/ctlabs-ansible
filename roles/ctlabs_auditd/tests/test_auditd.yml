---
# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_auditd/tests/test_auditd.yml
# Description : test playbook for ctlabs_auditd (syntax-check target)
# ------------------------------------------------------------------------------

- name: ctlabs_auditd.test.run
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
    ctg_virtype: kvm
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"
