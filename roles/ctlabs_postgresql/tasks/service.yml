---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_postgresql/tasks/service.yml
# Description : service
# ------------------------------------------------------------------------------

#- name: ctlabs_postgresql.tasks.service.disabled
#  service:
#    name : "{{ item }}"
#  loop: "{{ ctlabs_postgresql.defaults.service.disable }}"

- name: ctlabs_postgresql.tasks.service.installed
  systemd:
    name    : "{{ ctlabs_postgresql.defaults.service.name }}"
    enabled : true
    masked  : false
    state   : started
