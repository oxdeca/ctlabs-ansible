---

# ------------------------------------------------------------------------------
# File        : ctlabs_goflow2/tasks/package.yml
# Description : packages
# ------------------------------------------------------------------------------

- name: ctlabs_goflow2.tasks.package.redhat.install
  when: os_family == 'redhat'
  dnf:
    name              : "{{ ctlabs_goflow2.defaults.pkgs[os_family].url }}"
    disable_gpg_check : false
