---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_vault/tests/test_vault.yml
# Description : test playbook for ctlabs_vault (syntax-check target)
# ------------------------------------------------------------------------------

- name: ctlabs_vault.test.server
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
    ctlabs_vault_install_type: server
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"

- name: ctlabs_vault.test.agent
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
    ctlabs_vault_install_type: agent
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"

- name: ctlabs_vault.test.proxy
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
    ctlabs_vault_install_type: proxy
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"

- name: ctlabs_vault.test.cli
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
    ctlabs_vault_install_type: cli
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"
