---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_mariadb/tests/test_mariadb.yml
# Description : test playbook for ctlabs_mariadb (syntax-check target)
# ------------------------------------------------------------------------------

- name: ctlabs_mariadb.test.run
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"
