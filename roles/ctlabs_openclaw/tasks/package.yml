---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_openclaw/tasks/package.yml
# Description : packages
# ------------------------------------------------------------------------------

- name: ctlabs_openclaw.tasks.package.debian
  when: ctg_os_family == 'debian'
  block:
    - name: ctlabs_openclaw.tasks.package.debian.keydir
      file:
        path : /etc/apt/keyrings
        state: directory
        mode : '0755'
    
    - name: ctlabs_openclaw.tasks.package.debian.repos.key
      get_url:
        url : "{{ item.key }}"
        dest: "{{ ctlabs_openclaw.defaults.config.keydir }}/{{ item.name }}.asc"
        mode: '0644'
      loop: "{{ ctlabs_openclaw.defaults.repos[ctg_os_family] }}"
    
    - name: ctlabs_openclaw.tasks.package.debian.repos.file
      apt_repository:
        repo: "deb [signed-by={{ ctlabs_openclaw.defaults.config.keydir }}/{{ item.name }}.asc] {{ item.url }} nodistro main"
        update_cache: yes
      loop: "{{ ctlabs_openclaw.defaults.repos[ctg_os_family] }}"

    - name: ctlabs_openclaw.tasks.package.debian.nodejs.pin
      template:
        src : nodejs.pref.j2
        dest: "{{ ctlabs_openclaw.defaults.config.prefdir }}/{{ item.name }}.pref"
        mode: '0644'
        owner: root
        group: root
      loop: "{{ ctlabs_openclaw.defaults.repos[ctg_os_family] }}"
      notify: ctlabs_openclaw.handlers.apt.update_cache

    - name: ctlabs_openclaw.tasks.package.debian.update.cache
      apt:
        update_cache: true
    
- name: ctlabs_openclaw.tasks.package.pkgs.installed
  when: item.when is not defined or item.when
  package:
    name  : "{{ item.name }}"
    state : present
  loop    : "{{ ctlabs_openclaw.defaults.pkgs[ctg_os_family] }}"
  register: result
  until   : result is success
    
- name: ctlabs_openclaw.tasks.package.openclaw.installed
  npm: 
    name  : openclaw
    state : latest
    global: true
