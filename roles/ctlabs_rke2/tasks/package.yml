---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_rke2/tasks/package.yml
# Description : packages
# ------------------------------------------------------------------------------

- name: ctlabs_rke2.tasks.package.debian
  when: ctg_os_family == 'debian'
  block:
    - name: ctlabs_rke2.tasks.package.debian.update.cache
      apt:
        update_cache: true

    - name: ctlabs_rke2.tasks.package.install.name
      when: item.name is defined
      apt:
        name  : "{{ item.name }}"
      loop    : "{{ ctlabs_rke2.defaults.pkgs[ctg_os_family] }}"
      register: result
      until   : result is success

    - name: ctlabs_rke2.tasks.package.install.url
      when: item.url is defined
      apt:
        deb   : "{{ item.url }}"
      loop    : "{{ ctlabs_rke2.defaults.pkgs[ctg_os_family] }}"
      register: result
      until   : result is success

- name: ctlabs_rke2.tasks.package.rke2.install
  get_url:
    url  : "{{ ctlabs_rke2.defaults.pkgs.rke2.uri }}"
    dest : "{{ ctlabs_rke2.defaults.pkgs.rke2.path }}"
    mode : "{{ ctlabs_rke2.defaults.pkgs.rke2.mode }}"
  register: result
  until   : result is success
