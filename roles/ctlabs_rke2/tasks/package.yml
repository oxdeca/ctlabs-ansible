---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_rke2/tasks/package.yml
# Description : packages
# ------------------------------------------------------------------------------

- name: ctlabs_rke2.tasks.package.install
  package:
    name  : "{{ item.name }}"
    state : present
  loop: "{{ ctlabs_rke2.defaults.pkgs[ctg_os_family] }}"

- name: ctlabs_rke2.tasks.package.k3s.installed
  get_url:
    url  : "{{ ctlabs_rke2.defaults.pkgs.k3s.uri }}"
    dest : "{{ ctlabs_rke2.defaults.pkgs.k3s.path }}"
    mode : "{{ ctlabs_rke2.defaults.pkgs.k3s.mode }}"

- name: ctlabs_rke2.tasks.package.helm
  block:
    - name: ctlabs_rke2.tasks.package.helm.download
      get_url:
        url   : "{{ ctlabs_rke2.defaults.pkgs.helm.url }}"
        dest  : "{{ ctlabs_rke2.defaults.pkgs.helm.tmptar }}"
        #force : no
    - name: ctlabs_rke2.tasks.package.helm.untar
      unarchive:
        src  : "{{ ctlabs_rke2.defaults.pkgs.helm.tmptar }}"
        dest : "{{ ctlabs_rke2.defaults.pkgs.helm.tmpdir }}/"
        remote_src: true
    - name: ctlabs_rke2.tasks.package.helm.install
      copy:
        src  : "{{ ctlabs_rke2.defaults.pkgs.helm.tardir }}/helm"
        dest : "{{ ctlabs_rke2.defaults.pkgs.helm.path   }}"
        mode : '0750'
        remote_src: true
    - name: ctlabs_rke2.tasks.package.helm.argo.repo
      kubernetes.core.helm_repository:
        name    : "{{ item.name }}"
        repo_url: "{{ item.repo }}"
      loop: "{{ ctlabs_rke2.defaults.helm }}"
