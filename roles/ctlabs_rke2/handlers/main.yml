---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_rke2/handlers/main.yml
# Description : main handler
# ------------------------------------------------------------------------------

- name: ctlabs_rke2.handlers.server.systemd.reload
  systemd:
    name          : "{{ ctlabs_rke2.defaults.service.server.name }}" 
    enabled       : true
    daemon_reload : true

- name: ctlabs_rke2.handlers.service.server.started
  service:
    name  : "{{ ctlabs_rke2.defaults.service.server.name }}"
    state : started

- name: ctlabs_rke2.handlers.control.systemd.reload
  systemd:
    name          : "{{ ctlabs_rke2.defaults.service.control.name }}"
    enabled       : true
    daemon_reload : true

- name: ctlabs_rke2.handlers.worker.systemd.reload
  systemd:
    name          : "{{ ctlabs_rke2.defaults.service.worker.name }}"
    enabled       : true
    daemon_reload : true
