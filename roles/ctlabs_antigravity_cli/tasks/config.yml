---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_antigravity_cli/tasks/config.yml
# Description : configurations
# ------------------------------------------------------------------------------

- name: ctlabs_antigravity_cli.tasks.config.group
  group:
    name   : "{{ ctlabs_antigravity_cli.defaults.config.group }}"
    system : true

- name: ctlabs_antigravity_cli.tasks.config.user
  user:
    name   : "{{ ctlabs_antigravity_cli.defaults.config.user  }}"
    group  : "{{ ctlabs_antigravity_cli.defaults.config.group }}"
    system : true

- name: ctlabs_antigravity_cli.tasks.config.group.certs.member
  user:
    name  : "{{ ctlabs_antigravity_cli.defaults.config.user   }}"
    group : "{{ ctlabs_antigravity_cli.defaults.config.group  }}"
    groups: "{{ ctlabs_antigravity_cli.defaults.config.groups }}"
    shell : /bin/bash

- name: ctlabs_antigravity_cli.tasks.config.agy.dir
  file:
    path : "{{ ctlabs_antigravity_cli.defaults.config.dir }}"
    state: directory
    owner: "{{ ctlabs_antigravity_cli.defaults.config.user  }}"
    group: "{{ ctlabs_antigravity_cli.defaults.config.group }}"
    mode : '0750'

- name: ctlabs_antigravity_cli.tasks.config.profile.d
  template:
    src  : "{{ ctlabs_antigravity_cli.defaults.config.profile.template }}"
    dest : "{{ ctlabs_antigravity_cli.defaults.config.profile.file     }}"
