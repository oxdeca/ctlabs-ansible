[Unit]
Description=Netbox Development Server
After=network.target

[Service]
KillMode=process
Type=forking
NotifyAccess=all
ExecStart=-/bin/sh -c '/usr/bin/netbox.sh start'
ExecStop=/usr/bin/pkill -f runserver
TimeoutSec=300

[Install]
WantedBy=multi-user.target

