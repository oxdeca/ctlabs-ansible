---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_netbox/handlers/main.yml
# Description : description
# ------------------------------------------------------------------------------

- name: ctlabs_netbox.handlers.service.restart
  service:
    name    : "{{ ctlabs_netbox.defaults.service.name }}"
    enabled : true
    state   : restarted
