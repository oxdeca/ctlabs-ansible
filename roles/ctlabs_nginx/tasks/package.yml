---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_nginx/tasks/package.yml
# Description : packages
# ------------------------------------------------------------------------------

- name: ctlabs_nginx.tasks.package.installed
  package:
    name  : "{{ item.name }}"
    state : present
  loop    : "{{ ctlabs_nginx.defaults.pkgs[ctg_os_family] }}"
  register: result
  until   : result is success
