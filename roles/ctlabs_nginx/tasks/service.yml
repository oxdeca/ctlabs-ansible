---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_nginx/tasks/service.yml
# Description : services
# ------------------------------------------------------------------------------

- name: ctlabs_nginx.tasks.service.running
  service:
    name    : "{{ ctlabs_nginx.defaults.service[ctg_os_family].name }}"
    enabled : true
    state   : started
