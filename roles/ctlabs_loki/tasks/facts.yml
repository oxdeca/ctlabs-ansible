---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_loki/tasks/facts.yml
# Description : write local facts
# ------------------------------------------------------------------------------

- name: ctlabs_loki.tasks.facts.write
  tags:
    - setup
    - ctlabs_loki.facts
  template:
    src : facts.json.j2
    dest: "{{ ctg_facts_dir }}/ctlabs_loki.fact"
