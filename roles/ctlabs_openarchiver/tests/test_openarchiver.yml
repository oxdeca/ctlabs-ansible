---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_openarchiver/tests/test_openarchiver.yml
# Description : test playbook for ctlabs_openarchiver (syntax-check target)
# ------------------------------------------------------------------------------

- name: ctlabs_openarchiver.test.run
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    ctg_os: debian12
    ctg_os_family: debian
    oa_env_exists: true
  roles:
    - role: "{{ playbook_dir | dirname | dirname }}"
