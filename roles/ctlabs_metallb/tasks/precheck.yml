---

# ------------------------------------------------------------------------------
# File        : ctlabs-ansible/roles/ctlabs_metallb/tasks/precheck.yml
# Description : prechecks
# ------------------------------------------------------------------------------

- name: ctlabs_metallb.tasks.precheck
  block:
    - name: ctlabs_metallb.tasks.precheck.os.supported
      assert:
        that     : ctg_os is in ctlabs_metallb.os
        fail_msg : "OS[{{ ctg_os }}] is not supported!"
  rescue:
    - name: ctlabs_metallb.tasks.precheck.skip.unsupported
      meta: end_host
